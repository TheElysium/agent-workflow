# Normalize all text files to LF in the repository and on checkout.
# Prevents Windows CRLF checkouts from silently breaking the NTFS
# hardlinks between this repo and the live tool configs (see README,
# "Cross-tool sync notes" — a save that changes line endings replaces
# the inode just like a temp-file+rename save does).
* text=auto eol=lf
